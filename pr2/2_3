val n = readLine()!!.toInt()
val array = ArrayList<String>()
repeat(n) {
    array.add(readLine()!!)
}

for (i in 0 until n) {
    for (j in i + 1 until n) {
        if (array[i] == array[j]) {
            println("Same symbols:")
            println(array[i])
        }
    }
}
